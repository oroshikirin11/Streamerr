# Secrets — Owncast stream key, Jellyfin API key, server addresses.
# The repo must stay publishable; nothing here should ever be committed.
config.json
.env

node_modules/

# Runtime state
data/
cache/
chain/
run/
*.log

# Built UI and its toolchain
web/node_modules/
web/build/
web/.svelte-kit/

# Runtime overlay uploads
overlays/

# Local agent/session config
.claude/
